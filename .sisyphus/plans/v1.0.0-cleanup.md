# AriaEngine v1.0.0 クリーンアップ計画

## Context
AriaEngine v3.0 言語拡張は完了（29/29 tasks done）。現状は機能的に動作するが、本番リリース（v1.0.0）としてコード品質が不十分。デバッグ出力の残骸、未解決 TODO、未配線 CLI コマンド、空 catch ブロックなどが残っている。

## TODOs

- [x] **T1. Console.WriteLine デバッグ出力の一掃**
  - 99箇所の `Console.WriteLine` / `Debug.WriteLine` を特定
  - 開発用ログ → `_logger.LogDebug()` へ移行（または削除）
  - CLI ツールの進捗表示は `Console.Error` または専用プログレス API へ
  - テストコード内の出力は必要に応じて残す

- [x] **T2. TODO/FIXME コメントの解決**
  - `OptimizedBytecodeVM.cs` (5件)、`ErrorReporter.cs` (2件) など
  - 実装可能なものは実装、将来対応のものは Issue 番号付きで記録

- [ ] **T3. aria-save CLI コマンドの配線**
  - `Program.cs` のコマンド登録部分を確認
  - `aria-save` / `aria-load` が未登録なら登録
  - SaveManager と連携して動作確認

- [x] **T4. 空 catch ブロックの修正**
  - `catch { }`（エラーを握り潰す）パターンを検出
  - 最低限 `reporter.Report(...)` または `_logger.LogError(...)` を追加
  - 回復不能な場合は再throw

- [ ] **T5. 最終検証**
  - `dotnet build src/AriaEngine` → 0 warnings, 0 errors
  - `dotnet test src/AriaEngine.Tests` → 180/180 pass
  - `dotnet run -- --run-mode dev --init init.aria` → 正常起動・ゲームループ実行

## Verification
```bash
dotnet build src/AriaEngine
dotnet test src/AriaEngine.Tests
cd src/AriaEngine && dotnet run -- --run-mode dev --init init.aria
```

## Commit Strategy
- `chore(cleanup): remove debug Console.WriteLine artifacts`
- `fix(cli): wire aria-save/aria-load commands`
- `fix(error-handling): replace empty catch blocks with proper logging`
- `chore(todo): resolve or ticket remaining FIXME comments`

## Definition of Done
- [ ] 0 `Console.WriteLine` in production code (tests/tools exempt if justified)
- [ ] 0 unresolved TODO/FIXME in core engine files
- [ ] aria-save CLI works end-to-end
- [ ] 0 empty `catch { }` blocks
- [ ] Build clean, all tests pass, engine runs
